node_modules/
dist/
.next/
out/
*.tsbuildinfo

# Build/test output at the repo root only — NOT source directories that happen
# to be named the same thing (packages/core/src/coverage/ is real source).
/coverage/
/out/

# Secrets — never commit
.env
.env.*
!.env.example

# Batch output / caches
/kits.json
/kits.json.partial
.cache/
*.log
